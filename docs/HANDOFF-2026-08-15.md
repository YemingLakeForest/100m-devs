# Handoff — 2026-08-15

Scope: **a roadmap, and the first slice of the phase it opens.** The project-wide status, the
flag table and traps 1–27 are still in [`HANDOFF.md`](HANDOFF.md),
[`HANDOFF-2026-08-13.md`](HANDOFF-2026-08-13.md) and
[`HANDOFF-2026-08-13b.md`](HANDOFF-2026-08-13b.md). This file does not repeat them.

**As of the tip of `main`.** `npm run check`: lint, typecheck, **98 test files / 1,481 tests**, the
37-colour art gate, and **the browser gate at 48 screens**. All green.

> **Correction to the previous handoff, and to my own first report of this session.** The
> browser gate was **already red** when this session started — `UPGRADES` and `PARADIGM` clipped
> by `hud__right` at 898×403. I ran it, read an exit code from a run that produced no output,
> and reported it green. It is green now, and §26.1.8 requires it to stay that way.

---

## Read this first

**[`GDD.md` §26](../GDD.md) is the build order** — three phases, a closing gate on each, and no
dates. §0.2b points at it from the top of the document. It exists because the last three
sessions each landed four or five correct systems and the result on a handset was §25.7: the
gap between the document and the build has not been a specification gap for some time.

**[`GDD.md` §3.1](../GDD.md)** is the other half of the same correction: every place the loop
is asked to make room for an offer now lives next to the loop. `MONETISATION.md` keeps prices,
revenue mix, eCPM and store phasing — none of that is design.

---

## What landed

### The documents

| | |
|---|---|
| **§26** | The roadmap. Phase 1 the core loop, Phase 2 scale, Phase 3 life — each with a gate written as things a player does |
| **§3.1** | Monetisation folded onto the loop. Three collisions and §3.1.3's late-game inventory cliff, which neither document could see alone |
| **§13.12** | Run-length targets, and §13.12.2's resolution of the conflict with §21's four minutes: the trap is a *prologue*, so the table starts at Run 2 |
| **§13.13** | Levels. XP is never spent — it reaches levels, a level grants a point, a point buys a node |
| **§21.7.6** | **The instrument rule.** A system enters the game in the hands of the person who solves it |
| **§7.8.12** | The executive suite, whose walls arrive around the desk the founder never left |
| **§7.8.13** | A hero is the one person on the floor whose behaviour never changes |
| **§22.9** | The card: a laminated staff pass designed by somebody who badly wanted it to be a trading card |
| **§13.6.5a** | The story roster is not bought. This removed a wall across the middle of Phase 1 |
| **§25.8** | The intake ledger, R51–R80 |

Superseded rules were amended **in place** at §4.15, §13.6.7a, §13.10, §21.0c — §25.8.4 records
why that is now a standing rule.

### The build

| | What | Where |
|---|---|---|
| **Levels** | XP → levels → points, closed-form, with a relative tolerance because the closed-form sum and an accumulated series disagree in the last bits | `sim/heroXp.ts` |
| **The board** | Rebuilt: six-node chains, DEPTH before REACH, orthogonal **elbow** routing. The old Cloud branch ran `(1,1) (2,2) (3,3)`, which is a diagonal wearing grid coordinates | `sim/heroTree.ts` |
| **`nodeWeight`** | The line that makes §13.9.1 true. Full value at home, half away; **James is half everywhere**, which is his class as a number | `sim/heroTree.ts` |
| **The roster** | Six people resolved from `meta` and the run — levels, points, reach, coverage, and the fold | `sim/heroRoster.ts` |
| **The instrument rule** | `unlocksFor(shifts, arrived)`. The floor is §21.0c's shift; the ceiling is who has walked through a door | `game/unlocks.ts` |
| **Placement** | Anchor + reach, `heroPlacements` in the run save, XP accrual and the fold wired into `tick` | `game/store.ts`, `game/save.ts` |
| **The card** | §22.9's pass, and `HeroFace` drawing §7.8.7's own geometry as SVG | `hud/HeroCard.tsx`, `hud/HeroFace.tsx` |
| **The skill board** | §11.4's grammar with different nouns | `hud/HeroTree.tsx`, `render/heroIcons.ts` |
| **The roster strip** | §13.11.2, and the **interim** door — see below | `hud/Roster.tsx` |
| **The rails** | §10.1a's give-way order moved one step earlier, plus 3px of slab room at the rail | `styles/app.css`, `styles/tokens.css` |
| **`personPalette.ts`** | Hair, skin and shirt tables out of `room.ts`, because the card draws a person in the DOM | `art/personPalette.ts` |

---

## What Phase 1 still owes

> **Read this ordering as the ordering.** An earlier draft of this file listed the loop
> rebalance fourth, as a two-line measurement chore, between a rendering gap and a missing
> event system. That was wrong: §26.1.1 says the upgrade, prestige and founder trees carry the
> whole of the game's hour-scale, and everything else in Phase 1 decorates them. **A game that
> is not fun as boxes and lines will not be fun with rings and bells.**

### 1. The loop — §26.1.1, §14.8. Measured, part-fixed, and not finished

**Measuring it did not find a pacing mismatch. It found that there was no loop.** Eight runs,
attended, at three pokes a second:

| run | devs / cap | BP | wall |
|---|---|---|---|
| 3 | 182 / 292 | 29 | hire-cost |
| 6 | 181 / **376,315** | **29** | hire-cost |
| 8 | 166 / **1,118,474** | 23 | hire-cost |

Headcount flat at ~180 while §4.2's cap climbed four orders of magnitude; BP pinned at 29;
runs 3 through 8 identical. The cause is one line of §14.8.1: **income is linear in headcount
and the price of a head was exponential in it**, so the crossing point had nothing to do with
the cap and raising the cap raised nothing. Which is §6.1.3 inverted — the game's claim is that
comm tech dictates capacity, *not cash*, and cash was dictating it.

**What landed.** §13.7.1's founder tree gains **Recruiting** (`M-CLOUD`, *"You Know A Guy"*):
each level halves the hire-cost growth step, so affordable headcount doubles. It is the
founder's because that tree is `meta` and survives a Paradigm Shift — a run restarts with no
people and no cash, and keeps what you personally learned about hiring. It borrows §13.9.2's
Cloud spine, which is the exact joke: Melany buys capacity she did not have to organise;
the manager's diluted copy just knows someone.

After: headcount 181 → **2,842**, BP 29 → **89 and climbing**, revenue $37M → **$1.2B**, and
runs 7–8 outgrow the two-hour measurement horizon on their own. **Run 1 is untouched to the
cent** — the growth base defaults to §4.10a's 1.08, so §21's ladder and `runOne.test.ts` read
exactly as before.

**What is still wrong — §14.8.5, and it is the next thing anybody should pick up.** Runs 3–5
are **four-minute plateaus at ~100 developers** against §13.12.1's fifteen-to-forty-five-minute
band. The wall is not the hire curve (at a growth base of 1.02 the hundredth developer costs
seven dollars) — it is **cash flow**: §4.10e pays a project out over a long tail and payroll is
charged every second at once, so a studio growing through 100 heads pays $4,900/sec for revenue
that has not arrived. Candidates in order: the tail's spike share, the wage against the terminal
project's $137.50/SP, and §21.0a's seed round being the game's only cash injection.

`src/game/pacing.test.ts` is the instrument. **Measure before changing any of them.**

### 2. §7.8.12's executive suite

**This is the one substantial gap and it is deliberate.** The suite needs Pixi geometry (glass
walls anchored to `founderDeskPosition()`, one desk per arrived hero, nameplates), a new pick
class threaded through `stage.ts`'s tap pipeline, and §7.8.13's six signature idle behaviours.
Half-building it would leave the room in a worse state than the document — which is the failure
§26.0 exists to prevent.

Everything under it is built and tested: the roster, the levels, the coverage maths, the card
the suite opens, and the placement API (`placeHero` / `recallHero`) the drag will call.

**Until it exists, `hud/Roster.tsx` is doing double duty as the door**, and its module comment
says so. §13.11.2's strip is canon and survives — it answers *"who is idle?"*, which the world
cannot — but §7.8.12 is where a player is meant to reach these people, by looking at them. The
`TEAM` button goes away when the room arrives.

### 3. Placement has an API and no gesture

`placeHero(id, rung, index)` is wired, saved, tested, and drives the whole fold. Nothing calls
it but tests. The gesture is §7.8.9's existing carry, out of the suite onto a unit — so this is
part of item 1 rather than separate from it.

### 4. §13.11.1's coverage is not drawn

*"Coverage is drawn, not stated"* is the load-bearing sentence of §13.6.6 and §13.8, and
`render/heroBadges.ts` already has the footprint maths. Nothing renders it. Also part of item 1.

### 5. Run lengths are measured now, and they are not in band

Answered by item 1's instrument rather than outstanding: Run 2 measures at **four minutes**, not
the twenty §13.12.2 worried about. §13.12.2 said that if it came in short the economy moves
rather than the table, and that is now a concrete job with numbers attached (§14.8.5).

### 6. §18's random events are not a system

Found while checking §3.1.6: the only thing in the build that mentions §18 is a Paradigm node
that switches one of its events off. This blocks §3.1.6 item 3 and all of §26.3.3.

---

## Traps 28–33 — for the register in `HANDOFF.md`

**28. A gate can be weakened by a change that has nothing to do with it.** §21.7.6 hid the three
backlogs behind their heroes, and `?full` — the fixture that exists to be *the worst frame the
HUD ever has to draw* — sets `paradigmShifts` and no milestones. So the worst-case frame quietly
became three readouts emptier than the frame it reproduces, and the rail budget passed for the
wrong reason. That is §25.7.3's fourth hole wearing a new coat. **When a gate changes what it
shows, check the fixture in the same commit.**

**29. Padding a clipping rail is right vertically and wrong horizontally.** `overflow: hidden`
clips to the padding box, so `padding-bottom` gives a button's offset slab room inside the clip.
`padding-inline` does the opposite: the rail's children are already sized to its *content* box,
so horizontal padding narrows that box and pushes them out. Measured at 640×360 with 14px of it,
the offer panel was 230px wide in a 208px content box and hung fourteen pixels off the rail's
left edge — a defect the gate did not have before the padding was added. The sideways overhang
belongs on the one control tall enough to have any.

**30. A give-way rule keyed to width does not fire on a short, wide frame.** §10.1a has a stated
order in which rail content gives way and it was entirely correct; every step of it waited for
`max-height: 400px`. At 898×403 the rail wanted 436px in 381 and *nothing gave way*, because 403
is on the wrong side of 400 by three pixels. Moving the step to 470 — the breakpoint above it,
already in the file — closed 436 → 381 exactly. **Check that a give-way order actually fires at
every frame in the box, not just at the bottom of it.**

**32. A percentage height on a grid item resolves against its grid *area*, and an auto-sized
track containing one is cyclic — so the cap silently does nothing.** `.herotree__frame` asked
for `height: calc(100% - 2 * var(--edge-y))` inside a `display: grid; place-items: center`
host, measured **482 px inside a 436 px box**, with its `BACK` button off the bottom of the
frame. Nothing warns: the declaration is valid, it computes, and the computed value is simply
the content height. A flex item's percentage height resolves against the flex container's
content box instead, which is definite whenever the host is inset to the frame — so both hero
surfaces are `display: flex` and say why in the CSS. **If a height cap on a centred panel
appears to be ignored, look at whether its parent is a grid before you look at the number.**

**33. A modifier wired to the readout and not to the transaction passes every unit test.**
§13.7.1's Recruiting node was added to `nextHireCost` — what the HUD *shows* — and not to
`quote`, which is what the game actually *charges*. Types clean, tests green, tooltip correct;
the pacing harness bought four levels, printed a growth base of 1.005, and measured a studio
still stalled at 185 developers. **If a cost curve has two entry points, both take the modifier
or neither does** — and a lever wired to a readout is worse than no lever, because it is a lie
the interface tells confidently. Found only by simulating a run; nothing smaller could see it.

**31. `{n}{' '}{word}` has the accessible name `"1POINT"`.** `Button` wraps each child in its own
element, so JSX whitespace between two expressions is not in the computed name. It renders
correctly and is unfindable by `getByRole`. Build a label as one template string.

---

## Blocked on a human

- **§3.1.4 — where CONTRACTOR SURGE lives.** MONETISATION's workhorse placement wants a
  persistent control and §10.1a says both rails are full. Three options are costed in §3.1.4;
  the recommendation is to attach it to the ship moment instead of making it furniture, because
  it is the only one that costs no pixels. **This is the one monetisation item Phase 1 cannot
  close on its own.**
- **A flake I could not reproduce.** `Hud.test.tsx > "keeps reading while the buff lasts"` failed
  once in five full-suite runs and passed 16/16 in isolation and 4/4 full runs afterwards, on
  both sides of this session's changes. The test's own comment records a historical 1-in-3 flake
  at the same site from a different cause. Recorded rather than fixed, because I could not
  reproduce it and a speculative fix to a green test is worse than a known flake.
- Unchanged: the ElevenLabs key, §4.5a's two pacing knobs, §4.14's three weights, the
  poke-at-forty question, and F1.3's age rating.
