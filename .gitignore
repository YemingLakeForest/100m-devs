# Environment / secrets
.env
.env.*

# Editor directories and files
.idea/
.vscode/
*.swp
bash.exe.stackdump

# OS
.DS_Store
Thumbs.db

# Logs
logs
*.log

# Node (for when implementation starts)
node_modules
dist
dist-ssr
*.local
coverage/

# Snapshot APKs and the phone address deploy.sh caches — build outputs, and a
# machine's own LAN, neither of which belongs to anybody else's checkout.
.apk/
.adb-wifi-ip

# Git worktrees created by Claude Code. Full repo checkouts -- never commit these.
.claude/worktrees/

